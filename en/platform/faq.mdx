---
title: "FAQ"
description: "Frequently asked questions about using ruoli"
---

<AccordionGroup>
  <Accordion title="Low cache hit rate or unexpectedly high cost in Claude Code?">
    It's almost always the default attribution header — every request carries a dynamic session ID that blows out the upstream prompt cache.

    In CC Switch → **Edit Provider → Config JSON**, add one line to `env`:

    ```json
    "CLAUDE_CODE_ATTRIBUTION_HEADER": "0"
    ```

    Recommended whether you're on native Claude or a third-party model. See [Disable Attribution Header](/en/tools/claude-code#disable-attribution-header-required) for details.
  </Accordion>
</AccordionGroup>
